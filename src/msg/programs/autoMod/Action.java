package msg.programs.autoMod;

public enum Action {

    DISCONNECT, // Disconnects the user from the server
    LOGOUT, // Logs the user out
    MUTE, // Mutes user who sends the message
    DELETE_MSG, // Deletes message

}
